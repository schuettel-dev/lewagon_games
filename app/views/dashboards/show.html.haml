= render PageComponent.new do |c|
  - c.heading("Dashboard", :play)

  - c.body do

    .flex.flex-col.space-y-10
      = link_to "Increment #{@count.to_i}", "#", "data-controller": "counter", "data-action": "click->counter#increment", class: "block bg-black text-white"

    HERE COMES THE BODY

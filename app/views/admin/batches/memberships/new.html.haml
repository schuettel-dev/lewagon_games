= render PageComponent.new do |c|
  - c.heading("Add member", :user)

  - c.body do
    .w-full
      = turbo_frame_tag :batches__search_users_component do
        = render Batches::SearchUsersComponent.new(batch: @batch, search_query: params[:search_query])

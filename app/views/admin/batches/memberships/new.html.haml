= render PageComponent.new do |c|
  - c.heading("Add member", :user)

  - c.body do
    .w-full
      = turbo_frame_tag :batches_add_member_form_component do
        = render Batches::AddMemberFormComponent.new(batch: @batch, users_search_query: params[:user_search_query])

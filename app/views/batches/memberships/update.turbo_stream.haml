- update_membership_component = Batches::UpdateMembershipComponent.new(membership: @membership)
= turbo_stream.replace(update_membership_component.to_dom_id) do
  = render update_membership_component

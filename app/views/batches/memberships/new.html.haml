= render PageComponent.new do |c|
  - c.heading("Add member", :user)

  - c.body do
    .w-full
      = turbo_frame_tag :batches__add_member_form do
        = render Batches::SearchUsersComponent.new(batch: @batch, search_query: params[:search_query]) do |c|
          = c.search_users
